'use strict';

var registerController = require('controllers/register');

var name = 'controllers.main.register.register';

registerController(name, ['$scope',
function($scope) {
   
}]);

module.exports = name;