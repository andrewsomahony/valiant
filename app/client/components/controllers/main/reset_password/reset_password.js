'use strict';

var registerController = require('controllers/register');

var name = 'controllers.main.reset_password';

registerController(name, ['$scope',
function($scope) {
   
}]);

module.exports = name;