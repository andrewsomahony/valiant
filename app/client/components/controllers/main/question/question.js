'use strict';

var registerController = require('controllers/register');

var name = 'controllers.main.question.question';

registerController(name, ['$scope',
function($scope) {
   
}]);

module.exports = name;